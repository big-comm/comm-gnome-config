#!/bin/bash
# Final save shares the application's stable lock and committed generations.
set -euo pipefail
for attempt in {1..3}; do
    if /usr/bin/python /usr/share/comm-gnome-config/dconf_persistence.py stop; then
        exit 0
    fi
    sleep 0.5
done
exit 1
